spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/jcp
    username: root
    password: 'jennyboo1234'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    redis:
      host: localhost
      port: 6379